# 通用校招简历自动填写需求文档

> 版本：v0.4
> 日期：2026-08-22
> 状态：混合语义链路已接入并通过本地回归，真实站点待用户验收
> 适用范围：互联网公司、银行、国企/央企的国内校园招聘网页

本文件定义产品需求与验收边界。与 `DESIGN.md` v0.1 冲突时，以本文件和
`DEVELOPMENT_PLAN.md` 为准。

## 1. 背景

现有实现以模拟银行表单和模拟 Moka 组件为主要测试基础，采用“扁平字段扫描 +
别名规则匹配 + LLM 兜底 + 通用控件写入”。本地 E2E 可以通过，但真实知乎 Moka
页面出现以下问题：

- 规则阶段几乎没有产生有效填写；
- 字段标签大量为空，LLM 只能依赖 placeholder 猜测；
- 学校、专业等搜索下拉只输入文字，未可靠点击候选项；
- 教育、项目、获奖日期无法处理拆分的年/月控件；
- 工作经历、实习经历等重复分区可能串位；
- LLM 显示“补填成功”不等于页面组件已提交该值；
- 统计把语义映射、写入尝试和组件确认混为一谈。

对真实知乎、阿里、中国电信页面，以及公开可访问的用友大易、银行和央企网申表单
调研后，可以确认招聘页面不存在统一 DOM 结构：

- Moka 使用远程搜索下拉、Portal 弹层、重复卡片和拆分日期；
- 阿里使用 Kuma 组件、分区保存、合并的“实习/项目经历”和聚合文本字段；
- 中国电信及大量 `hotjob.cn/wt/...` 页面使用用友大易体系，包含 AngularJS、
  Bootstrap selectpicker、级联省市、大量重复条目和 100+ 控件；
- 银行/国企还包含高中经历、家庭/亲属回避、奖惩、挂科/补考、附件和声明等字段；
- 北森登录后真实简历页面尚未获得有效脱敏样本，不能仅凭招聘首页或 iframe 宣称适配。

因此，产品必须从“控件级脚本”升级为“页面结构理解 + 语义规划 + 平台能力适配 +
可验证执行”。

## 2. 产品目标

### 2.1 核心目标

1. 用户维护一次结构化校招档案，可在不同招聘系统中安全复用。
2. 对互联网、银行、国企/央企的常见简历分区进行高精度填写。
3. 规则与 LLM 共同完成语义映射，LLM 是默认启用的一等规划组件，不只是兜底。
4. 将“找对字段”和“真正写入组件”分开验证，拒绝把输入尝试当作成功。
5. 永远只填不提交，保存、下一步、声明确认和投递动作由用户完成。
6. 缺失事实保持为空，不使用 LLM 编造学历、奖项、家庭、证件或经历信息。
7. 通过脱敏实页采集持续扩展站点覆盖，而不是依赖开发者拥有所有招聘账号。

### 2.2 非目标

- 不自动提交职位申请；
- 不自动点击“保存”“暂存并退出”“完成”“下一步”“确认投递”等动作；
- 不处理验证码、短信验证码或登录；
- 不自动接受声明、隐私协议或真实性承诺；
- 不绕过网站权限、风控或安全校验；
- 不承诺一次支持所有租户自定义字段；
- 不为档案中不存在的信息生成虚构答案；
- 不把逐公司硬编码 CSS 选择器作为主要扩展方式。

## 3. 产品原则

1. **精度优先于覆盖率**：错填比漏填更危险。
2. **结构先于语义**：先确定分区、条目、控件组，再决定字段含义。
3. **语义与执行解耦**：LLM 决定“填什么”，适配器决定“怎么可靠地填”。
4. **页面状态是权威结果**：只有组件已提交并可读回时才算成功。
5. **规则提供约束，LLM 提供泛化**：两者不是互斥路径。
6. **敏感数据最小化**：能只发标签和字段路径时不发值。
7. **实页验收优先**：模拟 fixture 只用于回归，不代表真实站点已适配。

## 4. 用户与典型场景

### 4.1 目标用户

- 参加秋招、春招和实习招聘的在校生；
- 同时投递互联网公司、银行和国企/央企的候选人；
- 需要在多个 ATS 重复填写相同档案的用户。

### 4.2 典型流程

1. 用户创建或导入校招档案并逐项确认。
2. 用户登录目标招聘网站，进入简历编辑页。
3. 插件扫描页面，形成结构化页面模型。
4. 规则层生成候选映射和控件形态提示，但不先写页面。
5. 本地先补齐重复条目，建立页面条目到 Profile 条目的显式路由，并生成脱敏组件 IR。
6. LLM 在一次请求中复审全部字段，只返回 path、transform 和 decision，不返回浏览器工具。
7. 本地双重校验路径、条目下标、控件类型、敏感策略和转换白名单后执行。
8. 最终重新扫描页面，分别统计 mapped、written、committed 和 verified。
9. 用户人工核对后自行保存或进入下一步。

## 5. 页面结构模型

### 5.1 必须采用层级模型

```text
PageModel
├─ adapterId
├─ sections[]
│  ├─ semanticCandidates[]
│  ├─ entries[]
│  │  ├─ entryIndex
│  │  ├─ entryKind
│  │  └─ fields[]
│  │     ├─ semanticHints
│  │     ├─ controlGroup
│  │     ├─ currentState
│  │     └─ validation
│  └─ actions[]
└─ globalActions[]
```

### 5.2 Section 要求

每个分区至少包含：

- 页面标题及完整标题链；
- 分区容器引用；
- 可能的 Profile 分区候选；
- 条目容器列表；
- 添加、删除、保存、下一步、提交等动作候选；
- 是否为合并分区，例如“实习/项目经历”；
- 是否为聚合字段分区，例如“奖励与荣誉”单文本框。

### 5.3 Entry 要求

- 条目数量必须通过 DOM 卡片/行容器直接识别；
- 不得再通过相同 label 的出现次数推测条目下标；
- 工作和实习即使共用 Profile 数组，也必须保留页面分区身份；
- 合并分区允许一个页面分区接收多个 Profile 类型；
- 添加条目前后必须重新识别条目容器并验证数量增长；
- 已存在的非空条目默认不覆盖，除非用户明确开启覆盖策略。

### 5.4 SemanticField 要求

语义信号至少包括：

- `label[for]`、祖先 label、ARIA label/labelledby；
- 可访问性名称；
- 表单行标题、同列文本、前序兄弟文本；
- 分区标题和条目标题；
- name、id、placeholder、title；
- 控件类别、是否必填、长度/格式约束；
- 选项文本；
- DOM 顺序、视觉相对位置；
- 页面或平台提供的字段错误提示。

纯图标、计数器、按钮文本和导航项不得作为字段 label。

### 5.5 ControlGroup 要求

一个语义字段可以对应多个物理控件。必须支持：

- 单文本、长文本、富文本；
- 原生 select、radio、checkbox；
- 搜索式 combobox；
- 级联省/市/区；
- 单日期；
- 起止日期；
- 开始年/月 + 结束年/月四控件；
- 单年/月两控件；
- “至今/在读”开关；
- 文件上传；
- 多值标签；
- 组件的 trigger、输入框、Portal 弹层和已选状态。

## 6. 简历数据模型

### 6.1 公共强类型字段

保留现有基本信息、求职意向、教育、经历、项目、论文、竞赛、奖项、学生工作、
语言、技能、证书、家庭和自我评价等结构，并补充真实页面高频字段：

- 高中教育经历；
- 学号、实验室、导师、交换生、保送、学制；
- 高考生源地、必修课平均分、挂科/补考信息；
- 专利及发表阶段；
- 家庭成员与亲属回避信息分离；
- 紧急联系人；
- 学生干部、奖学金等可派生事实；
- 招聘信息来源、是否接受调剂等求职字段；
- 需要人工确认的附件清单。

日期在进入档案时必须归一化，不把页面展示格式带入持久化数据：

- 单个日期原子只保存为 `YYYY`、`YYYY-MM` 或 `YYYY-MM-DD`；
- 日期区间分为 `startDate`、`endDate` 和 `endDateIsNow`，不得长期保存
  `2022-09 ~ 至今` 之类拼接字符串；
- “至今/在读/在职/进行中”只落在布尔字段，`endDate` 保持空；
- 文档抽取、备份导入、档案手工保存都经过同一个归一化入口；
- 无法可靠解析的旧值不得静默删除，应保留并提示校对；
- 页面所需的年/月四控件、日期区间文本和“至今”开关由投影层生成。

### 6.2 可扩展事实库

无法稳定纳入公共 Schema 的租户字段进入事实库：

```ts
interface ProfileFact {
  path: string
  label: string
  value: string | string[] | boolean
  valueType: 'text' | 'enum' | 'date' | 'number' | 'boolean' | 'list'
  sensitivity: 'normal' | 'personal' | 'sensitive' | 'restricted'
  source: 'manual' | 'document-import' | 'derived'
  confidence: number
  confirmed: boolean
}
```

- 未确认的导入事实不得用于敏感或高风险字段；
- 事实必须保留来源，便于用户回查；
- `extras` 不再只是无类型 KV，而是正式参与规划的事实源。

## 7. 数据投影与转换

规划器必须支持以下映射关系：

1. **一对一**：姓名 → 姓名输入框。
2. **一对多**：`2024-06` → 年控件 + 月控件。
3. **多对一**：多条奖项 → “奖励与荣誉”文本区。
4. **派生**：奖项中存在国家奖学金 → “是否获得国家奖学金=是”。
5. **合并分区**：实习和项目 → “实习/项目经历”。
6. **拆分分区**：Profile `experiences` 按 kind 分到工作或实习区。
7. **枚举规范化**：中共党员/党员、硕士/硕士研究生等。
8. **格式转换**：日期格式、薪资区间、地区名称、电话号码国家码。
9. **摘要生成**：结构化竞赛/奖项/论文转换为页面要求的短文本。

转换必须由白名单函数执行。LLM 只能选择转换类型并提出参数，不能返回任意可执行代码。

## 8. 规则与 LLM 协同策略

详细活动架构见 [AGENT_ARCHITECTURE.md](./AGENT_ARCHITECTURE.md)。规则、LLM 和执行器是同一条链路中的不同职责，不是互斥运行模式。

### 8.1 默认策略

启用 API 时，LLM 默认在**一次全页面请求**中复审全部字段，而不是仅处理规则未匹配项：

1. 规则层对每个字段生成 0~N 个候选路径、类型兼容性和置信度；
2. 页面模型提供分区、条目、控件组和选项上下文；
3. LLM 对规则候选执行保留、纠正、补充或放弃；
4. LLM 可以选择拆分、合并、派生和格式转换；
5. 本地规划器验证路径、条目下标、类型、敏感策略和转换白名单；
6. 只有通过本地验证的计划才进入执行器。

纯规则 `off` 模式继续可用，但仍经过同一本地执行器和最终读回。

### 8.2 单次语义复审

- 每次开始填写最多发起一次语义复审模型请求；
- 每个字段应得到 fill/keep-rule/replace-rule/manual/skip 之一；
- LLM 不得输出 ToolCall、选择器、坐标、脚本或原始 DOM 操作；
- 重复条目创建、下拉选项点击、日期拆分和验证全部由本地代码完成；
- 模型漏项或单项被拒绝时，仅该字段采用规则候选或 manual，本次有效模型决策继续保留；
- 只有本地 verifier 可以产生 verified 状态；
- 保存、下一步、声明、提交和投递没有自动执行入口；
- 敏感事实的真实值只能在本地执行阶段解析。

### 8.3 LLM 输入

一次发送完整的脱敏 `FormPageIR`，禁止只截取前 60 项。每个字段输入包含：

- 稳定字段 ID；
- 分区候选和真实标题；
- 条目 ID、条目下标和条目类型；
- label、placeholder、name、id、ARIA；
- 控件组类型；
- 合成的白名单组件 HTML、精确日期槽位和本地交互类型；
- 选项和格式约束；
- 当前是否为空、是否锁定；
- 规则候选路径、分数和理由；
- 可用 Profile 路径、中文含义和允许的转换类型。

### 8.4 LLM 输出

```ts
interface SemanticPlanItem {
  fieldId: string
  decision: 'fill' | 'keep-rule' | 'replace-rule' | 'manual' | 'skip'
  profilePaths: string[]
  transform: string
  confidence: number
  reason: string
}
```

要求：

- `confidence` 统一为 0~1；
- path 必须来自本地白名单；
- 条目下标必须存在；
- 不确定时返回 `manual` 或 `skip`；
- 不输出简历值、ToolCall、DOM 选择器或代码；
- LLM 请求失败时保留高置信规则计划，不中断整页填写。

### 8.5 LLM 占比与成本控制

- 新站点、未知标签、聚合/派生字段：LLM 必须参与；
- 已验证适配器中的精确标签 + 类型 + 分区命中：仍进入分区复审，但允许缓存结果；
- 同一站点相同字段签名可复用语义计划，执行仍需重新验证 DOM；
- 当前实现使用一次完整请求；超过模型上下文上限时必须做结构压缩，不能静默丢字段；
- UI 显示预计发送字段数和当前隐私模式。

## 9. 控件执行器

### 9.1 通用状态机

每个控件执行：

```text
locate → inspect current state → interact → wait for settle
→ relocate → read authoritative state → classify result
```

结果分为：

- `mapped`：已确定语义；
- `written`：已执行写入动作；
- `committed`：组件内部状态已接受；
- `verified`：重定位后读回正确；
- `manual`：需要人工处理；
- `failed`：明确失败。

只有 `verified` 才计入“已填”。

### 9.2 搜索下拉

必须：

- 绑定当前 trigger 与对应弹层，不能从全页面所有可见选项中选择；
- 输入后等待加载完成和目标选项出现；
- 精确匹配优先，其次同义和规范化匹配；
- 点击选项后等待弹层关闭或已选状态改变；
- 从选中项、隐藏模型值或组件回显读取权威状态；
- 原始输入文字存在不代表选中成功；
- 支持“允许自由输入”和“必须从列表选择”两种策略。

### 9.3 日期

必须支持完整日期、年月、起止日期、拆分年月和“至今”。执行后逐部分读回，不允许把
`至今` 强行写入日期输入框。

执行器优先消费档案中的结构化日期；旧版拼接字符串只能作为迁移兼容输入，不能成为
新档案的标准输出。

### 9.4 重复条目

- 通过条目容器数量决定是否添加；
- 添加后验证数量增加；
- 按页面要求决定时间顺序；
- 不得把工作经历填入实习分区；
- 不得因为某个字段未匹配而把已有条目数判断为零；
- 重复运行默认幂等，不继续增加重复卡片。

### 9.5 安全动作

动作分级：

- 自动允许：打开下拉、选择选项、添加必要的重复条目；
- 默认禁止：删除条目、上传文件；
- 永久禁止自动触发：保存、暂存、下一步、完成、提交、投递、真实性声明。

## 10. 平台适配器

### 10.1 适配器职责

适配器只描述平台结构和控件能力，不硬编码业务值：

- 站点/平台特征检测；
- 分区和条目容器识别；
- label 提取；
- 控件组构建；
- Portal/弹层关联；
- 添加条目动作；
- 组件权威状态读取；
- 保存/提交动作识别与禁止。

### 10.2 第一批适配器

1. `MokaAdapter`：真实知乎页作为验收样本；
2. `DayeeWTAdapter`：中国电信、伊利、九江银行、中国人寿/广发、中广核样本；
3. `KumaAdapter`：阿里校招简历页；
4. `GenericAdapter`：标准 HTML、Ant Design、Element、Arco；
5. `BeisenAdapter`：获得真实登录后脱敏结构再开始实现。

适配器状态必须区分 `research`、`fixture-verified`、`live-verified`，只有最后一级可以在
README 中宣称支持。

## 11. 脱敏实页采集

插件提供“采集页面结构”诊断模式，导出：

- URL 平台特征和路径模板；
- 分区、条目、字段、控件组关系；
- label、placeholder、选项和必要 class；
- 添加/删除/保存/提交动作类别；
- 控件交互前后的 DOM 结构变化；
- 可选的脱敏截图。

采集必须移除：

- 输入值和文本域内容；
- 手机、邮箱、证件号、姓名、地址等个人信息；
- Cookie、Token、请求头、localStorage/sessionStorage；
- 文件名和附件内容；
- 可恢复用户身份的 URL 参数。

采集结果用于生成 fixture 和适配器回归用例，不能用于还原用户简历。

## 12. 隐私与安全

### 12.1 隐私模式

- `off`：纯本地规则，不调用 LLM；
- `labels-only`：发送页面语义和 Profile 字段定义，不发送值；
- `with-values`：发送非敏感值以提高复杂映射和摘要质量。

任何模式下，证件号码、家庭成员姓名/电话、照片、附件、紧急联系人等敏感值不得发送。
敏感字段允许把“字段含义和路径”发给 LLM，以便映射，真实值仅在本地取用。

### 12.2 文档导入披露

当前“文件不上传”只代表原始文件未直接上传；若提取后的完整文本发送给 LLM，UI 必须明确
披露。实现必须选择以下之一：

1. 本地识别并脱敏受保护字段后再发送；或
2. 在发送完整文本前获得一次明确授权，并列出可能包含的数据类别。

不得继续同时宣称“完整文本用于 LLM 抽取”和“敏感值任何情况下都不外发”。

## 13. 结果报告与诊断

页面侧栏至少展示：

- 识别出的平台与适配器成熟度；
- 分区、条目、字段数量；
- 规则候选数、LLM 保留/纠正/补充/跳过数；
- mapped/written/committed/verified/manual/failed 数量；
- 每个失败项属于语义失败还是交互失败；
- 未匹配字段的页面 label、分区和控件类型；
- 是否创建了额外条目；
- 明确的“未保存、未提交”状态。

报告不得展示完整证件号、电话、家庭信息等敏感值。

## 14. 非功能需求

- 单个分区 60 个字段以上不得截断；
- 全页 150 个字段时仍能完整分区规划；
- 单次 LLM 请求失败不得阻断规则候选和本地安全决策；
- DOM 更新后 StableRef 必须可重新定位或明确失效；
- 执行重复运行应幂等；
- 所有适配器必须在 Chrome MV3 隔离环境运行，不注入远程代码；
- 生产构建不得包含真实简历、Token、Cookie 或脱敏前快照；
- 调试日志默认不记录字段值。

## 15. 验收标准

### 15.1 通用安全门槛

- 自动保存/下一步/提交/投递点击次数：0；
- 未提供事实的自动编造次数：0；
- 已有非空字段被意外覆盖次数：0；
- 重复运行新增重复卡片次数：0；
- 敏感值进入 LLM 请求次数：0。

### 15.2 单适配器验收

每个适配器必须同时满足：

1. 脱敏 fixture 回归通过；
2. 登录后的真实页面完成一次只填不保存验证；
3. 普通文本、枚举、搜索下拉、日期、重复条目均有读回断言；
4. 规则与 LLM 计划均可观察；
5. 语义映射正确率不低于 98%；
6. 已执行常见控件的 verified 比例不低于 95%；
7. 剩余字段明确归为 manual，而不是伪成功。

### 15.3 第一阶段真实验收页面

- 知乎 Moka 校招简历页；
- 阿里校招简历页；
- 中国电信校招简历页；
- 至少一个公开用友大易银行表单；
- 北森真实页面在取得脱敏样本后加入。

## 16. 调研依据

- Moka 校园招聘解决方案：https://www.mokahr.com/solution/campus
- 华为招聘 FAQ：https://career.huawei.com/reccampportal/portal5/faq.html
- 中国移动校招简历：https://job.10086.cn/personal/resume_campus.html
- 国家电网招聘公告：https://www.sasac.gov.cn/n4470048/n29955503/n31577349/n31577357/c32015592/content.html
- 阿里校招简历：https://talent.alibaba.com/personal/campus-resume?lang=zh
- 中国电信招聘：https://job.chinatelecom.com.cn/
- 伊利用友大易简历表单：https://yili.hotjob.cn/wt/yili/web/index/showNewResume
- 九江银行简历表单：https://www.hotjob.cn/wt/JJYH/web/index/showNewResume?lanType=1
- 中国人寿/广发简历表单：https://www.hotjob.cn/wt/chinalife/web/index/showNewResume
- 中广核简历表单：https://cgn.hotjob.cn/wt/CGN/web/index/showNewResume
